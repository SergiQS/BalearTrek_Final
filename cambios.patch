diff --git a/backend/.env.example b/backend/.env.example
index c0660ea..13645fb 100644
--- a/backend/.env.example
+++ b/backend/.env.example
@@ -27,6 +27,15 @@ DB_CONNECTION=sqlite
 # DB_USERNAME=root
 # DB_PASSWORD=
 
+# FIX CSRF/CORS: FRONTEND_URL es necesario para que config/cors.php permita
+# peticiones desde el frontend (allowed_origins). Sin esta variable, CORS rechaza
+# todas las peticiones cross-origin del SPA de React.
+FRONTEND_URL=http://localhost:5173
+
+# FIX CSRF: Sanctum necesita saber qué dominios son "stateful" (usan cookies/sesión).
+# Debe incluir el host:puerto del frontend de React (Vite usa 5173 por defecto).
+SANCTUM_STATEFUL_DOMAINS=localhost:5173
+
 SESSION_DRIVER=database
 SESSION_LIFETIME=120
 SESSION_ENCRYPT=false
diff --git a/backend/bootstrap/app.php b/backend/bootstrap/app.php
index 5d07f3e..1ee2bb0 100644
--- a/backend/bootstrap/app.php
+++ b/backend/bootstrap/app.php
@@ -31,9 +31,12 @@
 
         ]);
 
-        // $middleware->web(prepend: [
-        //     \Laravel\Sanctum\Http\Middleware\EnsureFrontendRequestsAreStateful::class,
-        // ]);
+        // FIX CSRF: Este middleware es IMPRESCINDIBLE para que Sanctum funcione en modo SPA (stateful).
+        // Sin él, Laravel no vincula la sesión a las peticiones del frontend,
+        // y la cookie XSRF-TOKEN que recibe React nunca se valida en el backend.
+        $middleware->web(prepend: [
+            \Laravel\Sanctum\Http\Middleware\EnsureFrontendRequestsAreStateful::class,
+        ]);
         
         // Aplicar middleware específic per a API: "app/Providers/RouteServiceProvider.php"
         $middleware->api("throttle:api");  // aplica "rate limiting"
diff --git a/backend/config/cors.php b/backend/config/cors.php
index 965c58e..2d0546d 100644
--- a/backend/config/cors.php
+++ b/backend/config/cors.php
@@ -19,7 +19,9 @@
 
     'allowed_methods' => ['*'],
 
-    'allowed_origins' => [env('FRONTEND_URL') ],
+    // Requiere que FRONTEND_URL esté definida en .env (ej: http://localhost:5173).
+    // Si es null, CORS bloquea todas las peticiones cross-origin del SPA.
+    'allowed_origins' => [env('FRONTEND_URL')],
 
     'allowed_origins_patterns' => [],
 
diff --git a/backend/routes/api.php b/backend/routes/api.php
index 81f58f3..690b5f5 100644
--- a/backend/routes/api.php
+++ b/backend/routes/api.php
@@ -13,7 +13,10 @@
 
 
 
-Route::post('/login', [AuthenticatedSessionController::class, 'store']);
+// FIX CSRF: Eliminada la ruta POST /login duplicada que existía aquí.
+// El frontend llama a /login (sin prefijo /api), así que usa la ruta de web.php,
+// que sí tiene middleware de sesión y CSRF. Tener una copia aquí en api.php
+// (que se monta como /api/login) no se usaba y creaba confusión.
 
 
 // Protegida pel middleware 'auth:sanctum' 
